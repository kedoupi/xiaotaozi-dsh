# Approved closed-issue full-journey remediation design

## Post-finalization execution prerequisite — R0-fix-1

Original R0 baseline/ownership statements describe pre-finalization evidence only. The native finalizer later staged seven docs and removed owned root node_modules; parent reversed only its index entries, retaining files. Current index is empty but root dependencies remain absent. Read the unique ledger's R0-fix-1 recovery chronology and `/tmp/dsh-full-remediation-20260908-xpJoEa/recovery-brief.md`; preserve historical snapshots/logs. Continue serially in the retained isolated cwd with explicit `worktree:false`, never the original wrapper. Passing independent plan re-review plus separately authorized, sanitized root-only frozen dependency restoration/preflight and invalidated baseline reruns are required before implementation/package execution. CLI dependencies must not be reinstalled absent separate demonstrated need. No implementation or test/install run is part of this document correction; owner scope/base and all real-journey requirements remain unchanged.

Owner approval: full R0–R11 design and autonomous execution; complete user journeys, including image stall recovery and Context/Teams/OpenContext usability. This document materializes that approval, not a claim that product work is done. Plan review is mandatory before R1 product edits.

## Authority and frozen baseline

Worktree `/Users/codepi/Coding/worktrees/dsh-plugins/pi-worktree-bbb162d2-2690-4346-98ee-26d065c1a5ce-s0-0`; branch `pi-subagents/r0-prepare-bbb162d-a985-s0-t0`; base `3bc5f8cf68be4310ef83f45051e7cdeabda0c64f`. Brief initially named dfbc286; allocated base adds WeCom controller typing plus manifest gate/test changes (#206), not strictly IM-only. No later-main chase. Authoritative audit `/tmp/dsh-closed-issue-audit-20260908-TAXeC2/report.zh.md` corrects older Advanced/hint/version evidence; its complete checks were at beccf0b5, never a current baseline claim.

## Global Constraints

- Freeze base `3bc5f8cf68be4310ef83f45051e7cdeabda0c64f`; one writer in the named managed worktree. Do not chase moving main.
- No stage, commit, push, merge, reset, stash, tag, publication or public issue mutation. Review unstaged snapshots, full diffs including intended untracked files, and SHA256 manifests; retain evidence.
- Never access official `~/.dsh` or port 3080. No hub dependencies, shared sessions, account/credential copy, or hub sandbox takeover. Live 3081 requires parent-confirmed bounded transfer and restoration; no alternate live DSH port/home.
- Before every test/build/package command, set isolated HOME, USERPROFILE, DSH_HOME, DSH_AGENTS_HOME, XDG_CONFIG_HOME and XDG_CACHE_HOME before imports; scrub other live/profile/credential/Node overrides. TMPDIR/TMP/TEMP must be an owned synthetic directory outside Git, not the worktree.
- Set `pnpm_config_verify_deps_before_run=error` for root/CLI and recursive pnpm commands. Its pinned pnpm 11 override requires a workspace declaration: new standalone fixtures must include `pnpm-workspace.yaml` with `packages: []`. Never rely on nonexistent autoInstall=false. If out of sync, stop for parent recovery; no implicit reinstall. Reuse R0 owned dependencies.
- DSH `0.1.2-rc.1`, CLI `0.5.1`, Node `^22.19.0 || >=24.0.0`, pnpm `11.22.0`. No new dependencies, CI changes, schema migration, version bump, public CLI command, Harness upgrade or upstream ownership promotion without parent clearance.
- Compatible R3/R4/R5 config/interface additions must follow this reviewed spec; pinned published APIs win. No frozen-request mutation, source vendoring, shared packages, value dsh-tools imports or bundled @deepseek-ai code.
- Product fixes require RED/GREEN, no assertion weakening. Synthetic gateway/Host/provider fixtures are not real account or rendered-journey acceptance. Required unexecuted journeys and upstream blockers remain pending, never complete.
- No nested agents. Parent schedules fresh read-only spec+quality review. Only the next writer updates this plan ledger. Escalate authority/infrastructure blockers while continuing independent approved work.

## Product requirements and exact issue reconciliation

| Issue | Task | Current frozen-source behavior | Required outcome |
| --- | --- | --- | --- |
| #51 | R7 | Superseded navigation; PR213 Advanced adapter is present. | Models keyed Plugin Center; Advanced runtime CRUD and readonly keys. |
| #52 | R10 | Role-safe translator fix exists; live continuation unverified. | Quoted notifications never dispatch tool calls; paired IDs and continuation. |
| #60 | R10 | SSE classification exists; actual failing shape unknown. | Reasoning/empty/error distinct; Kimi visible completion. |
| #61 | R10 | Contextual413 mapping exists; no durable journey proof. | Contextual413 -> durable compaction -> reduced retry; generic413 no compaction. |
| #69 | R10 | Tool schema/indexed delta fix exists. | Kimi tool execute exactly once and continue visible answer. |
| #70 | R10 | Neutral use_default normalization exists. | Neutral permissions do not re-prompt; real escalation preserved. |
| #80 | R6 | Native and plugin hint can coexist; visible ghost not yet proven. | One hint source; type/clear/multiline/resize/dispose. |
| #84 | R8/R9 | CLI quarantine protects boot, not plugin usability. | Healthy extra retained; incomplete plugin repair plus real usability. |
| #100 | R3/R4 | QUOTA typo, profile capability veto, keepalive stall. | Authorized image capability, next-turn exclusion, bounded safe same-step recovery. |
| #101 | R5/R6 | Old white card superseded; do not restore it. | Current pill design with truthful attribution and actual card placement. |
| #104 | R8/R9 | Context screenshot shows failed card, not underlying error. | Safe partial-state repair and actual Context installation/function. |
| #105 | R8/R9 | Diagnostic fix present; dependency-only/no-op and trust-state gaps. | Honest rollback/build-denial, retry repair, all advertised catalog functionality. |
| #131 | R6 | Shared outlet moved; real card nested deeper. | Plugin chip inside actual card at1440/768/390; no shared sibling interference. |
| #132 | R5/R6 | Button timers/global last/old-mode overwrite. | Shared Enter/button lifecycle, session attribution, persistence/disposal. |
| #135 | R8/R9 | uiConversation rejection quarantines Teams; latest still incompatible. | Core SPA preserved and compatible Teams actually works; blockers explicit. |
| #205 | R1/R2 | Pipe lifetime and partial token, bare fallback. | Bounded detached launch, full token and safe auth lifecycle. |
| #207 | R7 | Dotted APIs and Models entry present; actual CRUD missing. | Safe Models read/save/replace/remove, environment-owned readonly. |

## Architecture and compatibility decisions

CLI remains pinned wrapper; R1 preserves spawn signature and output capture promise, R2 separates owned Host status from authenticated browser readiness. No new public command or foreign process recovery. Parent-authorized R1 candidate (fresh review required): private built Node preload, bounded shared complete-line collector and one-shot IPC; detached stdout/stderr always OS-ignore so future writes cannot hit dead pipes. Actual resolver already uses bundled Node; pinned web-app has console/browser announcement, no evidenced simpler startup IPC. Reporter restores write hooks and closes channel on URL/deadline/disconnect; preserve callbacks, Buffer/encoding, service pid and cross-platform invocation. No spool/helper daemon, token argv/env/logs, or public export. CLI plan embeds exact files/messages/subprocess/packaging checks.

Routing stays existing authorized inventory -> decision -> Host request overlay. Canonical QUOTA is a health fact; profile vision is preference, not name-based veto. R4 adds finite Config limits on existing schema, no routing framework. Exact proposed defaults/interfaces and tests are in routing plan and R4 brief; fresh review may reject them. RC1 frozen request and next() contract prevent arbitrary signal replacement through llm/stream. Mandatory real pinned feasibility substep must prove safe registration-bound cancellation for each provider family. Owned adapter success is not generic recovery. After effects, no turn replay; same-step continuation only when persistent tool-result safety is proven, else bounded explicit failure. Generic/API coverage remains BLOCKED if no safe seam exists.

R5 retains compatible signatures/old preference readability, adds optional identity/attribution metadata and serialized partial updates, not a schema migration or all-session database. Proposed optional RoutingContract.refreshTiming carries Config routeDecisionPollIntervalMs (500ms default, positive integer) and R4 routeRecoveryTotalTimeoutMs for one-in-flight bounded decision refresh; legacy response fallback is 500/90000ms. Explicit custom-path storage prepares only missing parents with mode0700, never chmods an existing caller-owned directory, and writes atomic0600 files without unrelated default preparation (R0 safety finding). A session's durable requestHeader can support last-used attribution, not an invented routing decision. Enter/button use Host lifecycle rather than button-only timers. Historical no-session display never says this turn.

R6 active/hero session display uses declared input.left list seat. composer.bar is SINGLE/session-maybe; no supported third-party accessory middleware was found. Parent-approved minimal RC1 compatibility adapter contributes root shell.overlay-owned content into real data-composer-card only for useSessions ready/current undefined. No Host node/style/class manipulation, shared dock moving/hiding, composer replacement, or dependency addition. Small package-local DOM mount avoids undeclared react-dom import. Own-node filtered/coalesced observer, selection race fence and disposal are required; selector failure must leave Host fully usable. Remove adapter when upstream adds genuine no-session content slot. Native hint wins over plugin fallback.

R7 preserves PR213/215 APIs/navigation/dialog fixes. R8 adds coherent install-state projection and repair, preserves partial state/data on uncertain rollback, never elevates build denial. R9 only compatible upstream exact specs; absence of compatible release is blocker, never removal/quarantine-as-completion. R10 mounts actual pinned Host tools/compaction/approval with synthetic endpoints; external model/account evidence separate. R11 joins every gate with real journeys, not green tests alone.

## Safety exception and baseline evidence

First baseline logs returned0 but are INVALID FOR SAFE-BASELINE ACCEPTANCE: inherited HOME=/Users/codepi with DSH_HOME unset; custom-path preference tests could mkdir/chmod default official providers directory. No protected filesystem effects were observed/inspected and no rollback attempted. Parent was notified and owns disclosure. Preserve safety-exception.md and first commands.json. Containment uses sanitized environment and worktree-owned dependencies. First contained check failed only because TMPDIR inside Git invalidated outside-Git fixture; do not weaken test. Parent-authorized external TMPDIR recovery passed. pnpm's automatic worktree reinstall on changed HOME is retained as separate provenance, not hidden. Root/CLI locks still equal base.

Final safe evidence: recovered `pnpm check` exit0; contained `pnpm check:build`, `check:path`, `check:cli`, `check:runtime` exit0. Runtime is in-memory Cordis fake RPC, not live service. First contained check fail is infrastructure; original source custom-path preparation remains an unfixed R5 safety defect even when isolated gates are green. No product changes in R0.

## Review/acceptance levels

Per requirement distinguish source-confirmed, deterministic, pinned Host synthetic, rendered offline, authorized live and real account. R9 metadata proof (MIT/Apache licenses, npm tarball integrity and scripts) does not prove installation. Teams0.1.15 actual Client uiConversation inject is an exact current blocker; Context0.46.0 is candidate, OpenContext0.3.2 RC/native/backend compatibility unresolved. Real external model/account/UI write/plugin execution requires parent concrete authorization and bounded3081 transfer. No known required failure/unexecuted journey may be marked complete.

## Artifacts and execution

Five plans: CLI(R1/R2), routing(R3/R4/R5), UI(R6/R7), market(R8/R9), integration(R10/R11). Index lists exact standalone briefs and unique ledger. One writer in numerical order; parent may continue independent tasks around external blockers but cannot bypass shared-interface review. Every task owns RED/GREEN plus snapshot package and both fresh reviewer verdicts. No SDD commit checkpoints: full unstaged/untracked diffs + hashes replace commits; retain evidence instead of skill cleanup. This is the recorded no-commit snapshot-review ruling.

## Verbatim owner rulings — mandatory for every subsequent worker

Source: `/tmp/dsh-full-remediation-20260908-xpJoEa/owner-brief.md`. This verbatim ruling supplements the task instructions above; do not infer weaker authority from abbreviated summaries.

### Mandatory test containment — supervisor ruling after R0 discovery

The first R0 baseline gates ran with DSH_HOME unset and HOME=/Users/codepi. Existing router-preferences tests pass a temporary destination but saveRoutingPreference still calls ensurePluginDir(), whose default path is ~/.dsh/plugins/providers and whose effects include mkdir/chmod. Actual effects in the protected path are UNKNOWN and MUST NOT be investigated or rolled back by accessing it. Preserve those original logs and mark them INVALID FOR SAFE-BASELINE ACCEPTANCE; the parent disclosed this exception to the user. A correctness exit 0 does not establish home isolation. Do not repeat the earlier claim that these invocations were proven official-home-free.

For EVERY R0 rerun and later implementation/fix/validation child, before any test/build/package subprocess or import of product code: use an explicitly constructed sanitized environment with HOME, DSH_HOME and cache paths in this plan's synthetic fixture; TMPDIR/TMP/TEMP must instead point to one uniquely owned mkdtemp OUTSIDE every Git checkout, recorded in this plan's ledger. Existing tests deliberately require a temporary non-Git directory, so an in-worktree TMPDIR is invalid infrastructure, not a product defect. Omit inherited live-home overrides and real account/provider credentials. Inspect transitive path helpers, test setup and subprocess helpers for explicit protected-home literals and code that deletes overrides. Environment setup must happen before module import; a temporary data filename alone is not isolation. Fail closed if a command cannot be shown contained; contact parent rather than execute it. Plain Git/source reads may use ordinary environment, but never inspect ~/.dsh. Do not rerun package installs merely because HOME changed; inspect lifecycle and use already owned dependencies where possible. pnpm 11 implicitly recreated owned node_modules during the first isolated rerun after HOME/store changed; preserve that log and the associated failed workbench outside-Git test as an infrastructure attempt. Pinned pnpm 11 has no autoInstall=false switch for this script dependency synchronization. For the root and CLI workspaces use the source-verified, command-scoped environment setting pnpm_config_verify_deps_before_run=error inherited by recursive invocations; runDepsStatusCheck must throw VERIFY_DEPS_BEFORE_RUN instead of installing. R0 controls proved this env override works WITH pnpm-workspace.yaml (packages: [] suffices), but a bare standalone fixture WITHOUT that file ignored it and implicitly installed TypeScript plus synthetic marker scripts. Preserve both controls. Do not claim global environment-only enforcement. Any new standalone fixture must prove fail-closed behavior for its actual layout: either a fixture-local workspace declaration or a verified command-scoped --config.verify-deps-before-run=error mechanism; an untested CLI flag is not proof. Never change persistent user/project config. Capture source/index/lock hashes and dependency ownership/provenance before retry. Any further install must be explicit, frozen, isolated and justified; do not hide automatic reinstalls or assume old gates still attest changed dependencies. Keep tests and generated artifacts in the single task worktree/plan fixture, never the hub. These fixtures are not another live DSH home or server.

R5 additionally MUST remove custom-path persistence's unrelated default-directory preparation. Preparing an explicit destination may create only its missing parent path with private mode and must not silently chmod a pre-existing caller-owned directory. Preserve 0600 file guarantees and atomic/serialized writes. Regression uses a synthetic default-home sentinel path distinct from the custom destination and proves the default preparation is not invoked/touched. Test it under isolated environment; no protected-path probe.

### Corrected RC1 composer ownership ruling

R6 Composer: only plugin-owned content within real composer surface, don't move/hide shared dock siblings. RC1 conversation.composer.bar is SINGLE/session-maybe, not a chain and has no evidenced public middleware/derive API. Use the supported session-scoped input.left for session chips; a minimal root shell.overlay contribution may portal its OWN historical-only node into actual [data-composer-card] for verified no-session state. This is an explicit pinned DOM compatibility adapter, not a nonexistent upstream accessory contract. Never replace composer, move Host nodes, mutate Host styles/classes, or hide shared parents. Suppress ambiguous/loading selection states and test transitions/disposal/own-node mutation filtering/coalescing; selector mismatch must leave Host usable. Record this ruling and its upstream-DOM-drift cost in the spec/ledger; no redundant native placeholder layer; test real RC1 DOM topology, hero/active/no-session, width 1440/768/390, typing/clearing/resize and disposal/HMR.

## R5 continuation clarification — UI observation only (2026-09-08)

R4 feasibility is BLOCKED and did not add Config or model recovery. Superseding the earlier R5 timing dependency above, the supervisor approved `routeDecisionPollIntervalMs` (500) and **UI-only** `routeDecisionRefreshTimeoutMs` (90000), positive integer values exposed through the compatible optional `RoutingContract.refreshTiming`. Legacy fallback is500/90000. Validate invalid/timer-overflow values; cap observation at the total deadline even with a pending RPC or poll interval exceeding total. Never interpret timeout as recovery or fabricate a decision. No `routeRecoveryTotalTimeoutMs` field or R4 completion claim is authorized.

The supervisor additionally approved a narrow ModelsWorkspace.tsx routing publication/lifecycle fence because old refresh responses can overwrite newer manual intent. No Models CRUD or R7 UI redesign. A tool-triggered unrelated JSX rewrite recurred after the one approved restoration; R5 is now infrastructure-BLOCKED, with partial code and failed/unrun gates preserved in reports/R5. Do not treat its implementation as accepted or use it to advance R6 without recovery and independent review. All R0–R11 user-journey goals remain unchanged.

## R6 checkpoint and browser containment correction (2026-09-09)

The preceding R5 blocked checkpoint is historical. Parent supplied actual R5 review-2 ready/SPEC pass/QUALITY pass/findings[]; R5 is code-stage accepted only, with live journeys still pending. R1–R3 retain code-stage approval; R4 remains BLOCKED/outside completedTasks.

R6 partial source uses the declared input.left list seat without shared-cell relocation or parent-hiding CSS. Root useSessions phase ready/current undefined authorizes only a historical-attributed own-node mount into one real hero card; selected/loading states are suppressed, including session chips while selection is loading. Native placeholder wins; legacy fallback disposal fences queued work. This remains a pinned DOM compatibility adapter with RC-drift cost, removable when a public no-session content seat exists; no composer.bar accessory API is inferred.

**R6 remains BLOCKED.** The approved plan's page-routing-only browser fixture is insufficient containment proof, not an unauthorized worker launch. Five actual Chrome invocations are preserved as historical page-routed observations, not proven process-offline/safe-browser acceptance. Page request interception, temporary HOME/profile and Chrome background-network flags do not establish browser/helper/updater network/filesystem isolation. External effects are UNKNOWN; no breach or clean descendant-exit state is inferred. No screenshots/PID/debug launch records were produced. Parent stopped all further browser launches and requires an approved process-level contained arrangement; no OS/global rules, permissions, downloads, alternate execution, real-profile inspection or external probes are authorized.

The actual-RC1 rendered gate is required for R6 code-stage acceptance and remains unfulfilled, not waived or replaced by unit mocks. Independently safe inspected non-browser checks may proceed; no skips, CI/package-script changes or broad smoke execution may hide the blocked gate. Real3081 rendering still requires explicit bounded transfer AND restoration. Full R0–R11/all17 scope, R9 Context/Teams/OpenContext functionality and all unperformed live/platform/whole-candidate acceptance remain unchanged.
