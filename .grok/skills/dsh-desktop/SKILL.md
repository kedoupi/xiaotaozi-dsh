---
name: dsh-desktop
description: >
  Refuse new Xiaotaozi Desktop work. The tree is gone (git tag archive/desktop).
  Use when the user mentions 改壳, 托盘, sidecar, tauri, dmg, 公证, 种子,
  发行版, or runs /dsh-desktop.
---

# dsh-desktop

There is **no desktop client** in this tree. The user product is `xtz`. Do not recreate `apps/desktop/`, do not `tauri build`, do not notarize, do not ship a `.dmg` or pack.

History: `git show archive/desktop`. Point the user at `xtz` / `xiaotaozi-env` / `xtz-cli`.

## Done

Say that Desktop was removed, that history is `archive/desktop`, and that you did not recreate it.
